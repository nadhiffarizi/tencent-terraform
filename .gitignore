# --- Tencent Cloud Secrets ---
# NEVER commit these files as they contain your SecretId and SecretKey
*.tfvars
*.tfvars.json
secret.tfvars

# --- Terraform Local State & Cache ---
# Local directory for providers and modules
.terraform/

# State files (contains your infrastructure's current configuration)
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.lock.info
backend_backup.tf

# --- Crash Logs ---
crash.log
crash.*.log

# --- Plan Files ---
# Ignore files generated by 'terraform plan -out=FILENAME'
*.tfplan

# --- OS Specific ---
.DS_Store
Thumbs.db
